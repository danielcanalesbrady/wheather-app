{
  "name": "weather-app-m3",
  "version": "1.0.0",
  "scripts": {
    "sass:watch": "sass --watch scss/main.scss:assets/css/main.css",
    "sass:build": "sass scss/main.scss:assets/css/main.css --style compressed",
    "dev": "npm run sass:watch"
  },
  "devDependencies": {
    "sass": "^1.66.1"
  }
}
